{
  "routes": [
    {
      "method": "GET",
      "path": "/users/balance-history",
      "handler": "user.getBalanceHistory",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/users/btc-wallet",
      "handler": "user.getBtcWallet",
      "config": {
        "policies": []
      }
    }
  ]
}
